export interface User {
    id?: string;
    email: string;
    name?: string;
    role?: string;
  }
export interface admin {
    id?: string;
    email: string;
    name?: string;
    role?:"admin";
  }
